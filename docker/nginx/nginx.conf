events {
    worker_connections 1024;  # ワーカーが処理できる接続の最大数を指定します。
}

http {
    # フェーズ1: ドメイン未取得のためIPアクセス・HTTPのみ有効化。
    # フロントもAPIもこの1つのserver blockでパス経由で振り分ける。
    server {
        listen 80;
        server_name _;

        location /api/ {
            proxy_pass http://api:8080;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        location / {
            proxy_pass http://web:3000;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }
    }

    # フェーズ2: ドメイン取得後、DNSをVPSに向けてcertbot等で証明書を取得したら
    # 上のserver blockを削除し、以下を有効化する（sumaslo-analyzer.devの箇所は実際のドメインに置換）。
    #
    # server {
    #     listen 80;
    #     server_name sumaslo-analyzer.dev api.sumaslo-analyzer.dev;
    #     location / {
    #         return 301 https://$host$request_uri;
    #     }
    # }
    #
    # server {
    #     listen 443 ssl;
    #     server_name sumaslo-analyzer.dev;
    #
    #     ssl_certificate /etc/nginx/ssl/sumaslo-analyzer.crt;
    #     ssl_certificate_key /etc/nginx/ssl/sumaslo-analyzer.key;
    #
    #     location / {
    #         proxy_pass http://web:3000;
    #         proxy_set_header Host $host;
    #         proxy_set_header X-Real-IP $remote_addr;
    #         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    #         proxy_set_header X-Forwarded-Proto $scheme;
    #     }
    # }
    #
    # server {
    #     listen 443 ssl;
    #     server_name api.sumaslo-analyzer.dev;
    #
    #     ssl_certificate /etc/nginx/ssl/sumaslo-analyzer.crt;
    #     ssl_certificate_key /etc/nginx/ssl/sumaslo-analyzer.key;
    #
    #     location / {
    #         proxy_pass http://api:8080;
    #         proxy_set_header Host $host;
    #         proxy_set_header X-Real-IP $remote_addr;
    #         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    #         proxy_set_header X-Forwarded-Proto $scheme;
    #     }
    # }
}
